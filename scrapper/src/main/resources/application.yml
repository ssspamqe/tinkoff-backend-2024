app:
  scheduler:
    enable: true
    interval: 10s
    force-check-delay: 10s

  default-stack-overflow-url: https://api.stackexchange.com/2.3/
  default-git-hub-base-url: https://api.github.com/

spring:
  application:
    name: scrapper

server:
  port: 8080

logging:
  config: classpath:log4j2-plain.xml
